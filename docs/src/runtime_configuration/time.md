# `wutengine.time`

- `target_frame_rate`: The frame time to target, in nanoseconds
- `fixed_timestep`: The fixed timestep to use, in nanoseconds
- `maximum_frame_time`: The maximum frame time before the engine starts to treat a frame as an "unwanted pause", in nanoseconds.
