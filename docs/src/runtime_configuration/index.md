# Runtime Configuration

WutEngine provides support for setting some configuration values at runtime when the engine is loaded, instead of hardcoded compile-time configuration. These configuration values are commonly provided through a configuration file.

The configuration values are accessed through the main `wutengine` category, followed by the specific subcategory and config key.

The subpages that follow describe the currently supported configuration values.
